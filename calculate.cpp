#pragma once
#include <string>
#include"calculate.hpp"

using namespace std;

string calculateBullAndPgia(string choice,string guess){
  // int bull = 0;
  // int pgia = 0;
  // for (size_t i = 0; i < count; i++) {
  //
  // }
  // string ans = "bull : " + to_string(bull) + ",pgia : " + to_string(pgia);
  // return ans;
}
